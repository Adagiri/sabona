version: 1.0
runtime: nodejs18 
build:
  commands:
    build:
      - npx pnpm install
      - npx pnpm run db:generate
      - npx pnpm build
      - npx pnpm run db:deploy
run: 
  command: python app.py